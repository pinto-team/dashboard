import type { ApiResponse } from '@/shared/api/types'

export interface {{Entity}}Data {
{{#each fields}}
    {{name}}: {{type}}
{{/each}}
}

export interface Create{{Entity}}Request {
{{#each createFields}}
    {{name}}: {{type}}
{{/each}}
}

export type Update{{Entity}}Request = Partial<Create{{Entity}}Request>

    export type {{Entity}}Response = ApiResponse<{{Entity}}Data>
    export type {{Entity}}ListResponse = ApiResponse<{{Entity}}Data[]>
